# 小小国家 AI 开发约定

修改代码前，先阅读相关的现有代码和项目文档，优先沿用当前项目已有的命名、目录结构和实现方式。

制作或修改 Unit Prefab、Animator、`UnitDefinition` 或单位技能配置前，必须阅读 `Assets/Doc/UnitPrefabAuthoring.md`。可重复的 Unit 制作流程使用项目 Skill：`.agents/skills/create-unit-prefab/SKILL.md`。Unit 制作规范发生实质变化时，必须在同一次修改中同步更新 Skill，并保持两者标注的版本一致。

## 代码风格

1. 保持代码简单、直接、容易理解，不增加不必要的理解成本。
2. 不要为了未来可能出现的需求，提前增加抽象层、设计模式、兼容分支或通用系统。
3. 项目自有代码中不要添加 `try`、`catch`、`finally`。
4. 不要使用 `throw` 或异常来处理正常的业务流程。
5. 优先使用空值检查、`bool` 返回值、`Try` 风格接口、状态枚举、提前返回和必要的日志处理失败情况。
6. 修改范围保持最小，只实现当前明确提出的需求，不顺便重构无关代码。

## 第三方与自动生成代码

1. 上述代码风格要求主要针对项目自有代码，不要为了统一风格而修改第三方库内部实现。
2. 不要随意修改 YooAsset、UniTask、Unity Packages 或其他外部依赖的源码。
3. 不要手动修改 Unity Input System 等工具生成的代码。
4. 处理第三方 API、废弃提示或编译错误前，先确认项目实际使用的 Unity、Package 和 API 版本。
5. 未经明确要求，不要修改依赖版本、Package 配置或第三方源码。

## 实现原则

1. 优先满足用户当前明确提出的需求。
2. 已有简单实现能够满足需求时，不引入更复杂的替代方案。
3. 尊重工作区中已有的用户修改，不覆盖或撤销无关改动。
4. 如果确实需要违反以上约定才能完成任务，先说明原因并等待用户决定。

## 工具使用

1. 是否使用 Unity CLI 由 AI 根据任务自行判断；使用时控制输出和 Token 消耗。
